package com.stone.module.pet.ui.activity.splash

class SplashPresenter : SplashContact.Presenter() {

    override fun loadAdvData() {
        //        ApiHelper.splash()
        //                .compose(RxJavaUtil.schLife(mView))
        //                .compose(RxJavaUtil.<SplashEntity>dataObj())
        //                .subscribe(result -> {
        //                    if (result.isSuccess()) {
        //                        mView.showAdvImg(result.getData());
        //                    }
        //                }, error -> {
        //                    error.printStackTrace();
        //                    mView.showMsg(App.getAppContext().getString(R.string.system_error));
        //                    mView.onError();
        //                });

    }
}
